# 打开 StarComplete

**扩展面板里没有 Install from VSIX。不要去找，那条路在 Cursor 里走不通。**

插件已经可以用下面这一行装上（本机已装过一次：`starcomplete.starcomplete`）。

## 安装（只走终端）

打开「终端」应用，整行粘贴，回车：

```bash
/Applications/Cursor.app/Contents/Resources/app/bin/cursor --install-extension /Users/macbookpro/starcomplete/starcomplete-0.1.1.vsix
```

看到 `was successfully installed` 就对了。

然后回到 Cursor，按 **⌘⇧P**，输入并回车：`Developer: Reload Window`。

## 看星标

1. 按 **⌘⇧P**，运行 `Developer: Reload Window`（换过版本必须做）
2. 看窗口**右下角**，应出现 **★ StarComplete**。没有这几个字 = 扩展没起来
3. 打开 `/Users/macbookpro/starcomplete/extension/examples/playground.ts`
4. 如果已经是 `console.log();`，先把 `log()` 删掉，只留 `console.`
5. 光标放在点的右边：`console.|`
6. 按 **Control+Space**（键盘左下角 Control，不是 Command）
7. 列表**最上面**应出现 **★ log**、**★ error**、**★ warn**。左边带星才算成功

只看到 `assert`、`clear`、`count` 排在最上面、名字前面没有 ★，那是 TypeScript 自带的，还没成功。

## 列表里没有星标时

1. 按 **⌘⇧P**，搜 `StarComplete`，应能看到这个扩展
2. 再跑一次 `Developer: Reload Window`
3. 回到 `console.` 后面再按 Control+Space

## 扩展装不上时

用浏览器打开：`/Users/macbookpro/starcomplete/playground/index.html`

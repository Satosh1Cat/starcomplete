# 打开 StarComplete

克隆仓库、安装依赖之后，用仓库里的 VSIX 装扩展，再打开 playground 试星标。

## 安装扩展

先构建（若还没有 `starcomplete-0.1.1.vsix`）：

```bash
npm install
npm run build
cd extension
npx --yes @vscode/vsce package
```

在 **VS Code** 或 **Cursor** 里：

1. 命令面板（macOS：⌘⇧P，Windows / Linux：Ctrl+Shift+P）
2. 运行 **Extensions: Install from VSIX…**
3. 选中 `starcomplete-0.1.1.vsix`（仓库根目录或 `extension/` 下）

命令行（`code` 或 `cursor` 已在 PATH 中时）：

```bash
code --install-extension ./starcomplete-0.1.1.vsix
# 或
cursor --install-extension ./starcomplete-0.1.1.vsix
```

然后运行 `Developer: Reload Window`。状态栏应出现 **★ StarComplete**。

## 看星标

1. 运行 `Developer: Reload Window`（换过版本必须做）
2. 看窗口状态栏，应出现 **★ StarComplete**。没有这几个字 = 扩展没起来
3. 打开 `extension/examples/playground.ts`
4. 如果已经是 `console.log();`，先把 `log()` 删掉，只留 `console.`
5. 光标放在点的右边：`console.|`
6. 按 **Ctrl+Space**（macOS 是 Control，不是 Command）
7. 列表**最上面**应出现 **★ log**、**★ error**、**★ warn**。左边带星才算成功

只看到 `assert`、`clear`、`count` 排在最上面、名字前面没有 ★，那是语言服务自带的，还没成功。

## 列表里没有星标时

1. 命令面板搜 `StarComplete`，应能看到这个扩展
2. 再跑一次 `Developer: Reload Window`
3. 回到 `console.` 后面再按 Ctrl+Space

## 扩展装不上时

用浏览器打开仓库里的 `playground/index.html`。
